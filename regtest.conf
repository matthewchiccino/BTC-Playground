# Regtest node config for My BTC Playground.
# Chain is mined once by setup_chain.py, then frozen. Nothing else mines.
regtest=1
server=1
listen=0
txindex=1
fallbackfee=0.0001

rpcuser=btcplayground
rpcpassword=btcplayground

[regtest]
rpcport=18443
